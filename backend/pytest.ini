[pytest]
asyncio_mode = auto
addopts = --asyncio-mode=auto
# 缺陷 011：原先没有 testpaths，在仓库根目录裸跑 pytest 会连 backend/ 下的演示脚本
# （test_websocket.py 等）一起收集，这些脚本会发起真实外部调用并产生费用。
# 限定只收集 tests/，从源头避免"跑一次测试就把额度烧掉"。
testpaths = tests