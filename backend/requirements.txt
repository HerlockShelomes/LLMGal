# LLMGal 后端基础运行依赖（已在 CPython 3.14 验证导入）。
# FastAPI 0.95.x 需要 Pydantic 1.x；1.10.26 是该大版本的末个兼容修复版，
# 可避免 1.10.7 在 Python 3.12+ 的 ForwardRef 导入错误。
#
# 火山图像 SDK 不在基础环境中自动安装。其旧版上游依赖会在 Python 3.14 上触发
# pycryptodome 构建问题；只有确实启用 IMAGE_PROVIDER=volcengine 时再参考
# requirements-volcengine.txt 和 README 的兼容性说明。

fastapi==0.95.1
pydantic==1.10.26
uvicorn==0.21.1
websockets==11.0.2
requests==2.32.4
openai==1.93.2
