# LLMGal 后端运行时依赖（Python 3.11–3.14）。
#
# 仅保留 Connect.py -> Integration.py -> Text.py / Voice.py / Image.py
# 真实调用链会导入的包。数据库、Redis、Flask 等历史开发依赖不属于当前运行时，
# 因而不应阻塞首次部署。火山图像 SDK 请按需安装 requirements-volcengine.txt。

fastapi==0.95.1
pydantic==1.10.26
uvicorn==0.21.1
openai==1.93.2
requests==2.32.4
websockets==11.0.2
