# LLM Galgame 后端模块一测试用例（34 条）

被测对象：`code/LLMGal/backend` 的 WebSocket 消息处理、外部服务编排与共享资源状态管理。除特别说明外，LLM、TTS、图片生成和图片下载服务均使用可控 Mock，不评价生成内容质量。

重要级别：P0=主流程或严重数据/协议风险；P1=重要异常/分支；P2=一般兼容性或健壮性。

| 测试用例编号 | 测试项（即功能模块或函数） | 测试用例标题 | 重要级别 | 预置条件 | 输入 | 操作步骤 | 预期结果 | 使用的测试方法 |
|---|---|---|---|---|---|---|---|---|
| TC-WS-01 | `Connect.websocket_chat` | 合法请求的静态资源完整主流程 | P0 | 服务已启动；`Records.txt` 中有 Wendy 合法记录且 index 为 3；Wendy 的 7 张情绪静态图片齐全；Mock LLM 返回 `(高兴)(收到邀请很开心)你好呀`；Mock TTS 返回合法 Base64 音频；客户端可接收响应。 | `type=client_query`，合法 `message_id`，payload 含 Wendy、有效模型、`realTimeRendering=false`、`ElderSister` 和 `{role:user,content:你好}`。 | 1. 建立 WS 连接。<br>2. 发送输入消息。<br>3. 接收服务端响应。<br>4. 在 ACK 等待窗口内发送同 `message_id` 的 JSON ACK。<br>5. 检查 Records 和音频目录。 | 返回一个 `assistant_response`，`message_id` 与请求一致，`status=success`；response 为 Mock LLM 全文；emotion 为 `happy`；index 为旧值 `3`；metrics 有非负 time_cost、tokens_used 等于 response 字符长度；imageUrl 为空；生成/覆盖 `Wendy_3_Stream.mp3`；Records 中 Wendy index 更新为 `4`。 | 场景法 |
| TC-WS-02 | `Connect.websocket_chat` | 非法 JSON 文本请求 | P1 | 服务已启动并建立 WS 连接。 | 非 JSON 文本，如 `{invalid-json`。 | 1. 发送输入。<br>2. 接收服务器返回。<br>3. 在同一连接继续发送一条合法请求并 ACK。 | 首次返回 `type=error`、`code=JSON_PARSE_ERROR`、message 为请求格式错误且附 detail；连接保持可用，后续合法请求仍可被处理。 | 等价类划分 |
| TC-WS-04 | `Connect.websocket_chat` | 非 client_query 消息类型 | P1 | 服务已启动并建立 WS 连接。 | 顶层 `type=canceled_request`，其余字段可为合法结构。 | 1. 发送输入。<br>2. 接收返回。<br>3. 再发送合法请求确认连接可复用。 | 返回 `type=error`、`code=INVALID_MSG_TYPE`；不调用 LLM/TTS/图片依赖；连接仍能接收下一条合法请求。 | 等价类划分 |
| TC-WS-05 | `Connect.websocket_chat` | 缺少顶层 type | P1 | 服务已启动并建立 WS 连接。 | 不含 `type`，含 message_id 和合法 payload 的 JSON 对象。 | 1. 发送输入。<br>2. 接收返回。 | 不会返回 `INVALID_MSG_TYPE`；因访问缺失键产生异常，服务端尝试发送 `SERVER_ERROR`。不应调用外部依赖。 | 等价类划分 |
| TC-WS-06 | `Connect.websocket_chat` | 缺少 payload | P1 | 服务已启动并建立 WS 连接。 | 含 `type=client_query`、message_id，不含 payload。 | 1. 发送输入。<br>2. 接收返回。 | 因访问 `message['payload']` 失败，服务端尝试发送 `SERVER_ERROR`；不调用外部依赖。 | 等价类划分 |
| TC-WS-07 | `ClientRequest` / `websocket_chat` | payload 缺少顶层必填业务字段 | P1 | 服务已启动并建立 WS 连接；外部依赖均为 Mock。 | `payload` 中遗漏 role（其他三个顶层 payload 字段存在）。 | 1. 发送输入。<br>2. 接收返回。 | Pydantic 构造 ClientRequest 失败；返回 `type=error`、`code=VALIDATION_ERROR`；不调用外部依赖。 | 等价类划分 |
| TC-WS-08 | `websocket_chat` 错误响应封装 | 错误响应与前端协议兼容性 | P0 | 服务已启动；准备能校验 `MessageType.isServerMessage` 的前端接收器或人工按接口定义检查。 | 触发任一格式错误，例如非法 JSON。 | 1. 发送输入。<br>2. 获取服务器错误响应。<br>3. 用前端 server-message 判定规则检查字段。 | 当前代码返回的 error 包含 type/code/message/detail，但不含 message_id/status/payload；前端 `isServerMessage` 判定为无效消息。该结果应记录为协议不一致问题。 | 场景法 |
| TC-CFG-01 | `process_query` / 嵌套配置读取 | textModel_config 缺 modelText | P0 | 服务已启动；建立 WS；Mock 不应被调用。 | payload 中 `textModel_config` 是字典但缺 `modelText`。 | 1. 发送请求。<br>2. 接收返回。<br>3. 检查 Mock 调用次数。 | ClientRequest 的外层 dict 校验可通过，但 process_query 读取内键时抛 KeyError；服务端先发送 PROCESS_ERROR，随后因 resp 未定义可能再发送 SERVER_ERROR；LLM 不应被调用。 | 等价类划分 |
| TC-CFG-02 | `process_query` / 嵌套配置读取 | imageModel_config 缺 realTimeRendering | P0 | 服务已启动；建立 WS；Mock 不应被调用。 | payload 中 `imageModel_config` 是字典但缺 `realTimeRendering`。 | 1. 发送请求。<br>2. 接收返回。<br>3. 记录所有响应。 | 同 TC-CFG-01：外层校验可能通过，读取内键后失败；观察 PROCESS_ERROR 后是否出现额外 SERVER_ERROR；不应调用 LLM。 | 等价类划分 |
| TC-CFG-04 | `Integration.request_confirmation` | 四类有效音色映射 | P1 | 直接调用业务函数或发送四次其他条件相同的请求；Mock TTS 记录 audio.voice_type。 | voiceCate 分别为 GirlFriend、BoyFriend、ElderSister、LiteratureGuy；LLM 返回 `(中性)(正常)回复`。 | 1. 对四个值分别执行请求。<br>2. 检查 TTS Mock 收到的 voice_type。 | 四个输入分别映射为源码定义的四种火山音色；请求都可继续完成。 | 等价类划分 |
| TC-CFG-05 | `Integration.request_confirmation` | 未知音色默认回退 | P1 | 正常资源；Mock TTS 记录音色。 | voiceCate 为 `UnknownVoice`、空串或大小写错误值（任选一种）。 | 1. 发送请求。<br>2. ACK。<br>3. 检查 TTS Mock。 | 不返回配置错误；TTS 使用 GirlFriend 对应的默认女声音色，主响应仍成功。 | 等价类划分 |
| TC-CFG-06 | `Text.get_llm_response` / 模型路由 | 未知文本模型的传递与失败 | P1 | Mock OpenAI 在收到未知模型名时抛出异常。 | modelText=`not-supported-model`，其余合法。 | 1. 发送请求。<br>2. 检查 OpenAI Mock 接收的 model。<br>3. 接收错误响应。 | 后端无模型白名单，未知值会原样传入 LLM；LLM 抛错后进入 PROCESS_ERROR 路径，并可能产生后续 SERVER_ERROR。 | 等价类划分 |
| TC-LLM-01 | `Text.get_role_prompt` | 角色 Prompt 读取并追加情绪指令 | P0 | 创建/隔离一份可读取角色文件，内容为可识别角色描述；Mock OpenAI 记录 messages。 | 合法角色和正常用户消息。 | 1. 调用主请求。<br>2. 检查 LLM Mock 的第一个 messages 元素。 | 第一条 messages 元素的 role 为 assistant；content 等于角色文件原文并追加固定 Emotion_Prompt，其中含 7 个中文情绪候选、原因不超过 30 字、整体不超过 150 字的指令。 | 场景法 |
| TC-LLM-03 | `Integration.Response_Collection` | LLM 输出无括号时的情绪默认值 | P0 | Mock LLM 返回 `普通回复`；Mock TTS、静态图片成功。 | 正常请求。 | 1. 发送请求并 ACK。<br>2. 检查 TTS 的 emotion 入参和响应 emotion。 | 情绪解析走 `len(match)==0`；TTS emotion 为 neutral；响应 emotion 为 `neutral`；response 保留 LLM 原文。 | 边界值分析 |
| TC-LLM-04 | `Integration.Response_Collection` | LLM 输出仅一个括号 | P1 | Mock LLM 返回 `(悲伤)回复正文`；其他依赖成功。 | 正常请求。 | 1. 发送请求并 ACK。<br>2. 检查 TTS、图片调用和响应。 | 解析情绪为悲伤、原因为空；TTS emotion 为 sad；响应 emotion 为 `sad`；图片情绪原因使用空字符串。 | 边界值分析 |
| TC-LLM-05 | `Integration.Response_Collection` | LLM 输出两个以上括号 | P1 | Mock LLM 返回 `(惊喜)(因为收到礼物)(额外描述)正文`；其他依赖成功。 | 正常请求。 | 1. 发送请求并 ACK。<br>2. 检查图片/TTS Mock 入参。 | 仅取前两个括号：情绪为惊喜、原因为“因为收到礼物”；第三个括号不影响映射；TTS emotion 为 neutral，响应 emotion 为 `surprised`。 | 边界值分析 |
| TC-TTS-01 | `Voice.Voice_Generation_through_http` | TTS 成功 Base64 音频保存 | P0 | Mock `requests.post` 返回 HTTP 成功和含合法 `data` Base64 的 JSON；隔离音频目录存在写权限。 | 正常角色、index=`3`、文本。 | 1. 调用主请求或 TTS 函数。<br>2. 检查 POST payload。<br>3. 检查目标 mp3 内容。 | POST 使用 timeout=30，audio 含传入音色/情绪，request.text 为 LLM 原文；生成 `voice/{role}/{role}_3_Stream.mp3`，其字节内容等于解码后的 Mock 音频。 | 场景法 |
| TC-TTS-02 | `Voice.Voice_Generation_through_http` | TTS 超时或请求异常 | P0 | Mock `requests.post` 抛 `requests.exceptions.Timeout` 或其他 RequestException。 | 正常请求。 | 1. 发送请求。<br>2. ACK（若收到成功响应）。<br>3. 检查后续图片和最终 WS 响应。 | TTS 函数返回空串且不抛异常；业务继续执行图片和 Records 更新；当前代码仍可能返回 `assistant_response/status=success`，且没有音频成功标识。 | 场景法 |
| TC-TTS-04 | `Voice.save_audio_from_base64` | 非法 Base64 音频数据 | P1 | Mock POST 返回 `data` 为非法 Base64；音频目录可写。 | 正常请求。 | 1. 发送请求。<br>2. 观察音频写入结果与 WS 响应。 | Base64 解码异常被 `save_audio_from_base64` 捕获，函数返回空串；不应产生有效 mp3；主流程仍可能返回成功响应。 | 等价类划分 |
| TC-TTS-05 | `Voice.Voice_Generation_through_http` | TTS 返回非 JSON 格式 | P1 | Mock POST 的 `raise_for_status` 成功，但 `json()` 抛 JSONDecodeError/ValueError。 | 正常请求。 | 1. 发送请求。<br>2. 接收和记录错误响应。 | 当前 TTS 函数未捕获 JSON 解析错误；异常向上触发 PROCESS_ERROR 路径，并可能导致额外 SERVER_ERROR。 | 等价类划分 |
| TC-IMG-01 | `Image.static_images` | 静态情绪图片恰好 7 张 | P1 | 隔离图片目录中存在 7 个指定的 `{role}_{neutral/happy/sad/fear/angry/surprised/shy}.jpg`；Mock 图片 API 记录次数。 | `realTimeRendering=false`，正常 role 和 image model。 | 1. 发送请求并 ACK。<br>2. 检查图片 API Mock。 | `len(shared_files)==7`，不调用图片生成；imageUrl 为 `""`；业务继续更新 Records 索引。 | 边界值分析 |
| TC-IMG-02 | `Image.static_images` | 静态情绪图片缺 1 张时重生成 | P1 | 隔离图片目录中只有 6 个指定静态图片；Mock original_image_generation 与 emotional_bro 成功。 | `realTimeRendering=false`。 | 1. 发送请求并 ACK。<br>2. 检查图片 Mock 调用次序与次数。 | `len(shared_files)!=7`，先生成 neutral 图一次，再循环生成另 6 个情绪图；共形成 7 张静态图；返回初始图片 URL。 | 边界值分析 |
| TC-IMG-03 | `Integration.Response_Collection` / `Image.emotional_bro` | 实时图生图成功 | P0 | Records 中该角色有有效 Recent_Url 和 index=3；Mock LLM/TTS 成功；Mock Visual API 与图片下载成功。 | `realTimeRendering=true`，modelImage=`high_aes_ip_v20`。 | 1. 发送请求并 ACK。<br>2. 检查图像服务 form 和图片文件。<br>3. 检查 Records。 | 调用图生图，form 的 req_key 为 `high_aes_ip_v20`、image_urls 使用 Records URL；图片写至 `{role}_3.jpg`；响应 imageUrl 为新 URL；Records 写入新 URL、index 改为 4。 | 场景法 |
| TC-IMG-04 | `Integration.Response_Collection` | 实时图生图异常后回退文生图 | P0 | Records 有角色 URL/index；Mock `emotional_bro` 抛异常；Mock `original_image_generation` 返回新 URL；TTS 成功。 | `realTimeRendering=true`，任意图片模型。 | 1. 发送请求并 ACK。<br>2. 检查回退 Mock 调用。<br>3. 检查响应和 Records。 | 捕获实时图异常后调用 original_image_generation；情绪和原因被拼入文生图参数；若回退成功则返回新 URL 并更新 Records。 | 场景法 |
| TC-RES-01 | `Integration.Response_Collection` | Records 索引普通递增与音频槽一致性 | P0 | Records 中 Wendy index=8；静态图片完整；LLM/TTS Mock 成功。 | 正常静态模式请求。 | 1. 发送请求并 ACK。<br>2. 查看音频文件名、响应 index、Records。 | TTS 写入 `Wendy_8_Stream.mp3`；响应 index 为 `8`；Records index 更新为 `9`。 | 边界值分析 |
| TC-RES-02 | `Integration.Response_Collection` | Records 索引 9 回绕到 0 | P0 | Records 中 Wendy index=9；静态图片完整；LLM/TTS Mock 成功。 | 正常静态模式请求。 | 1. 发送请求并 ACK。<br>2. 查看音频文件名、响应 index、Records。 | TTS 写入 `Wendy_9_Stream.mp3`；响应 index 为 `9`；Records index 更新为 `0`，后续请求将复用 0 槽。 | 边界值分析 |
| TC-RES-03 | `Response_Collection` / `updateLinks` | Records 文件不存在的静态回退 | P1 | 临时移除/隔离 `Records.txt`；静态图片完整；LLM/TTS Mock 成功；确保上层写 Records 的预期路径不可用或可控。 | 正常请求，realTimeRendering 初始为 true。 | 1. 发送请求。<br>2. 检查图片调用选择。<br>3. 接收最终结果。 | 读取阶段捕获 FileNotFoundError，将 realTimeGeneration 改为 False、index 设为 0；TTS 使用索引 0。随后 `updateLinks` 写记录时因文件不存在的实际结果应记录：当前代码没有为写入缺失文件提供恢复逻辑，可能触发处理失败。 | 场景法 |
| TC-EX-01 | `Integration.Response_Collection` | Records 存在但角色记录缺失 | P0 | `Records.txt` 存在，但不含请求 role 的完整三行记录；外部依赖 Mock。 | role=`UnknownRecordedRole`。 | 1. 发送请求。<br>2. 检查 LLM/TTS Mock 是否被调用。<br>3. 接收错误。 | `re.findall` 得到空列表，访问 `matches[0]` 抛异常；当前代码不进入 FileNotFound 回退；process_query 失败，触发 PROCESS_ERROR 路径。 | 等价类划分 |
| TC-EX-02 | `Integration.Response_Collection` | Records 索引为非数字 | P1 | Records 中目标角色 index 为 `abc`；LLM/TTS/静态图片 Mock 成功。 | 正常请求。 | 1. 发送请求。<br>2. 记录 TTS 是否已写文件、最终响应和 Records。 | LLM 与 TTS 可在 `int(index)` 前已被调用；转换 `int('abc')` 时失败，最终进入 PROCESS_ERROR 路径。该用例用于验证“部分副作用后失败”。 | 等价类划分 |
| TC-EX-03 | `websocket_chat` | 客户端在处理/响应阶段断开 | P1 | 建立 WS；Mock LLM 可控地延迟或立即成功。 | 合法请求；在发送后或响应前主动关闭客户端连接。 | 1. 建立连接并发送请求。<br>2. 立即关闭连接。<br>3. 观察服务端日志和其他独立连接。 | 服务端捕获 WebSocketDisconnect 后退出该连接循环；不应导致其他连接关闭。当前同步外部调用是否仍继续完成，应按实际观察记录。 | 场景法 |
| TC-ACK-01 | `Connect.websocket_chat` | 正确 ACK 的处理 | P1 | 正常成功 Mock；建立 WS。 | 合法请求；随后发送 `{message_id: 请求ID}` ACK JSON。 | 1. 发送请求。<br>2. 接收 assistant_response。<br>3. 在 30 秒内发送匹配 ACK。<br>4. 再发送第二条合法请求。 | 服务端打印确认送达并继续接收；第二条请求可作为正常业务消息处理。ACK 内容的其他字段不被要求。 | 场景法 |
| TC-ACK-02 | `Connect.websocket_chat` | ACK 超时后连接继续使用 | P1 | 正常成功 Mock；建立 WS。 | 合法请求；30 秒内不发送 ACK。 | 1. 发送请求并接收 assistant_response。<br>2. 等待超过 30 秒。<br>3. 发送第二条合法请求并按需 ACK。 | 服务端打印未收到 ACK，不重发、不改变已有 success 响应；循环继续，第二条请求可被正常处理。 | 边界值分析 |
| TC-ACK-03 | `Connect.websocket_chat` | 未 ACK 时下一条业务消息被误作 ACK | P0 | 正常成功 Mock；建立 WS；准备两个合法、不同 message_id 的请求。 | 第一条合法请求后，在 30 秒窗口立即发送第二条合法请求，不先发送 ACK。 | 1. 发送第一条并接收 response。<br>2. 立即发送第二条 client_query。<br>3. 等待超过 ACK 窗口并观察返回。 | 服务端将第二条 JSON 读入 ACK，因 message_id 不匹配仅打印 ACK 不匹配；第二条不会进入业务处理，也不会收到对应 assistant_response。这是当前协议状态风险。 | 场景法 |
| TC-CON-01 | `Records.txt` 与音频/图片共享资源 | 两客户端同角色并发导致状态覆盖 | P0 | 两个独立 WS 客户端；同一角色 Records index=3；Mock 外部调用设置可交错完成；隔离资源目录可检查。 | 两客户端同时发送 role=Wendy 的合法请求。 | 1. 同时建立 A/B 连接。<br>2. 近同时发送请求。<br>3. 接收响应并 ACK。<br>4. 检查 Records、`Wendy_3_Stream.mp3`、图片文件及两个响应。 | 由于无会话隔离或文件锁，两请求均可能读取 index=3，并写同名资源；最终 Records/文件由后完成者覆盖或发生丢失更新。应记录是否出现跨客户端资源串扰。 | 场景法 |
