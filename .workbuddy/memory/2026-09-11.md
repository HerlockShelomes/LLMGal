# 2026-09-11

## 模块一测试脚本环境审计（只读）

- 审计 `scripts/run_module1_tests.sh` 在当前 Windows 环境能否运行，产出指南 `docs/module1/run_module1_tests_环境适配指南.md`（新增文件，未改动任何已有文件）。
- 结论：不能运行。两个致命点：
  1. 第 12–14 行 `command -v python3.11` 失败 → `exit 1`（本机没有名为 python3.11 的命令）。
  2. 第 11/17/24 行假设 POSIX venv 布局 `bin/python`，Windows `venv` 只产出 `Scripts/python.exe`。现存 `.venv-module1` 即证据（只有 Scripts/，且 site-packages 只有 pip+setuptools 空壳）。
- 实测解释器矩阵：3.11.9 通过（32 passed, 2 xfailed）；3.12.8 与 3.13.14 均在收集阶段崩于 fastapi 0.95.1 + pydantic 1.10.7 的 `ForwardRef._evaluate() missing ... 'recursive_guard'`。
- 已否证的非问题：`pytest.ini` 的 `--asyncio-mode=auto` 在 pytest-asyncio 1.0.0 中仍有效；`--cov=*`、`tee`、`PYTHONDONTWRITEBYTECODE=1` 均正常。
- 修正版脚本（双布局探测 + `py -3.11` 探测链 + 3.11 版本断言）已在真实 Git Bash 实测跑通，源码见指南附录 A，**尚未落到 `scripts/`**。

## 本机可用 Python 3.11 基座（备忘）

- `C:\Users\Lenovo\AppData\Local\Programs\Python\Python311\python.exe` = 3.11.9（`py -3.11` 指向它）
- `E:\Anaconda\envs\vue-fastapi\python.exe` = 3.11.13
- 无 `python3.11` 命令；`python3` 只在 WorkBuddy 托管目录里且是 3.13，**不可用于本项目**。

## 环境坑（复用价值高）

- 沙箱内 Bash 工具的 PATH 缺 Git 的 `/usr/bin`，`ls`/`dirname`/`mkdir`/`head`/`tee` 都不可用；需要 coreutils 时用 `"/c/Program Files/Git/bin/bash.exe" -c '...'` 起真实 Git Bash。
- 沙箱拦截 `wsl.exe`，无法探测 WSL 是否安装。
