# 2026-09-12

## 执行策略 A：加固并跑通 run_module1_tests.sh

- 改写 `scripts/run_module1_tests.sh`（+94 / −11）：解释器探测链（`LLMGAL_TEST_PYTHON` → `python3.11` → `py -3.11` → 校验过的 `python3`）、venv 双布局探测（`bin/python` 与 `Scripts/python.exe`）、3.11 版本断言、依赖自愈（`import pytest, fastapi, pydantic, requests, websockets` 探测，缺了才装）。
- **运行中暴露第三层坑**：Git Bash 的 MSYS 路径 `/f/LLMGal/...` 原生 Windows Python 不认，`pip install -r` 报 `No such file or directory`。修复：用 `cygpath -m` 派生 `PROJECT_ROOT_NATIVE` / `VENV_DIR_NATIVE`，凡传给原生解释器的参数（`-r`、`--junitxml`、venv 目标）用原生形式，bash 自己用的（`mkdir`/`cd`/`tee`）仍用 MSYS 形式。
- 结果：32 passed, 2 xfailed（首次 3.55s / 幂等重跑 2.45s），34 用例与文档一一对应，0 failure 0 error。覆盖率 Connect 73% / Integration 83% / Text 76% / Image 40% / Voice 31%，TOTAL 56%。
- 产物：`artifacts/test-results/module1-junit.xml`、`pytest-output.txt`，另 `backend/.coverage`（均被 .gitignore 覆盖）。
- 报告：`docs/module1/run_module1_tests_输出报告.md`。
- **未执行任何 git 提交**，用户要求本地跑完由其审核后再上传。

## 待办 / 待用户决定

- `.workbuddy/`（AI 记忆目录）是否加 `.gitignore` 或不提交 —— 已在报告中提示，未擅自改 `.gitignore`。
- `.venv-module1` 当前基座仍是 Anaconda 3.11.13；可删掉让脚本用 `py -3.11`（python.org 3.11.9）重建。
- 两个 XFAIL（TC-TTS-04 零字节音频、TC-EX-02 索引正则先排除非数字）需改 `backend/` 实现才能消掉。

## 缺陷定位与修正指南（源码未动）

产出 `docs/module1/LLMGal_backend_程序修正指南.md`。核心结论：

- **两个 xfail 都是真缺陷，已实测根因**：
  - TC-TTS-04：`base64.b64decode(s)` 默认 `validate=False`，实测 `b64decode('%%%%')` 返回 `b''` 不抛异常 → 写出 0 字节文件。改 `validate=True` 即可（`validate=True` 实测抛 `binascii.Error`）。
  - TC-EX-02：`Integration.py:119` 正则 `index:(\d+)` 对 `abc` 不匹配 → `matches[0]` IndexError，早于所有副作用。实测改为 `index:([^\n]*)` 后 `abc`/`3` 都能取到，`int()` 处自然抛 ValueError，符合文档预期。
  - 两者都带 `strict=True`，**修完必须同步删 xfail 标记，否则 XPASS 判失败**。
- **10 条 PASS 用例断言的是缺陷行为**（改源码会转红）：TC-WS-05/06（KeyError→SERVER_ERROR）、TC-WS-08（error 缺 message_id/status/payload，前端 `isServerMessage` 判非法）、TC-CFG-01/02/06（`Connect.py:237-241` except 分支缺 `continue` 导致 resp 未绑定 → 双错误）、TC-EX-01、TC-RES-03、TC-ACK-03（业务消息被当 ACK 吞掉）、TC-CON-01（无锁并发覆盖）。
- **覆盖率缺口 = 死代码**：`Connect.py:16-70`（WebSocketManager / inner_websocket_operation）与 `Voice.py:64-189`（test_submit / parse_response / Voice_Generation）全仓无调用方；`Voice.request_confirmation` 与 `Integration.request_confirmation` 同名不同语义。
- **真实 Bug（mock 测不到）**：`Text.py:54` `if delta.content != ""` 对 `None` 为真 → `answer_content += None` → TypeError，真实 OpenAI 流必崩；`Image.py:83` 正则里 `\0` 是 NUL 笔误，实测会吃掉换行后全部文本。
- **架构级**：`Connect.websocket_chat`（async）直接调同步 `process_query` → 阻塞事件循环；7 处 `../frontend/src/assets/...` 相对路径依赖 cwd=backend；`updateLinks` 在静态模式下把 `Recent_Url` 清空（现网 Records.txt 三个角色 URL 已空，缺陷已发生）。
- **工程卫生**：`backend/TestSubject.py` 是火山 ECS 示例且 `from __future__` 在第 5 行 → 已验证 SyntaxError；6 处 API 密钥硬编码入库（Voice/Text/Image/TestSubject2/image_emotions）；`pytest.ini` 无 `testpaths`，直接跑 `pytest` 会收集 `backend/test_websocket.py` 连真实 localhost:8000 并产生真实计费调用。
- 前端（待确认）：`ClientChat.ts:86-88` / `UseChatClient.ts:81` 用 `imageMode_config`，与 `MessageType.ts:40`/`ChatView.vue:507`/`Connect.py:138` 的 `imageModel_config` 不一致。

修正分 4 阶段，见指南第 6 节。用户要求先给指南、等其指示再改源码。

## Bug 修复（只修 2 个 xfail，其余缺陷未动）

用户指示：先修脚本报出的 bug，不提交 github，出对比报告；其他缺陷保持原样但在报告中说明理由与后果。

**改动（生产代码仅 2 处核心逻辑）**
- `backend/Voice.py` `save_audio_from_base64`：`b64decode(audio)` → `b64decode("".join(audio.split()), validate=True)` + 空数据守卫。去掉 `"".join(split())` 会把带换行折行的合法 Base64 变成失败（回归），所以剥离空白是必须的兼容步骤。
- `backend/Integration.py:119`：`index:(\d+)` → `index:([^\n]*)`，让 `abc` 能被取出，改由下游 `int(index)` 抛 ValueError。
- `backend/tests/test_service_and_state_cases.py`：删 2 行 `@pytest.mark.xfail(strict=True, ...)`。**必须删** —— strict 模式下用例通过会 XPASS 判失败。

**结果**：32 passed + 2 xfailed → **34 passed / 0 failure / 0 error / 0 skipped**。覆盖率 TOTAL 56%→57%，Voice.py 31%→33%。junit：tests=34 errors=0 failures=0 skipped=0。

**实测对比方法（可复用）**：用 `git show HEAD:<path>` 把旧版源码导出到 `%TEMP%\llmgal_probe\ba\`，配合 `importlib.util.spec_from_file_location` 加载新旧两个模块，cwd 设在沙箱 `backend/` 下让相对路径 `../frontend/...` 落到沙箱内，同一组输入跑两遍对比返回值/异常/落盘字节数。合法载荷回归用了两种形态（无换行、每 8 字符折行），确认没引入回归。

**报告**：`docs/module1/LLMGal_backend_bug修复对比报告.md`（第 5 节是用户点名要的"未修缺陷为何要修 + 不修会怎样"）。

**未提交任何 git commit/push**，HEAD 仍 `0c401fe`。

## 代码还原 + 完成缺陷报告书

用户改主意：不急着修，先把代码还原，改为按 `G:\研究生阶段文件\软件测试\附录2：缺陷报告模板.doc` 出一份缺陷报告清单。

- **还原**：`git checkout -- backend/Voice.py backend/Integration.py backend/tests/test_service_and_state_cases.py`，`backend/` diff 归零，脚本重跑到 **32 passed, 2 xfailed**（回到未修复基线）。修复版已备份在 `%TEMP%\llmgal_probe\fixed_backup\`。
- `scripts/run_module1_tests.sh` **保留加固状态**（还原它就没法跑脚本了）。

**产出**：`docs/module1/LLMGal_测试缺陷报告书.docx`（模板副本填写，共 13 张表：修订记录 + 参考资料 + 11 张 18×11 缺陷表）。用户原件 mtime 保持 2021-09-27 未被改动。

11 条缺陷：001 非法 Base64 零字节 mp3（TC-TTS-04）、002 非数字索引异常类型与位置错误（TC-EX-02）、003 错误响应不符前端契约（TC-WS-08）、004 异常后未 continue 双错误（TC-CFG-01/02/06）、005 ACK 吞业务消息（TC-ACK-03）、006 并发槽位覆盖（TC-CON-01）、007 Records 缺失写回失败（TC-RES-03）、008 角色记录缺失抛 IndexError（TC-EX-01）、009 TTS 失败仍 success（TC-TTS-02）、010 覆盖率 56%（Voice 31%/Image 40%）、011 pytest 缺 testpaths 会打真实外部服务。

**本地 Word 模板填写的关键坑（重要）**
1. `tencent-local-office-edit` 的 `open_file` 返回的 `file_id` 是**被规范化的路径字符串**（如 `F://LLMGal//docs//...`），后续所有 `doc_*` 必须用这个字符串。`get_pool_status` 用来从 present_files 后的实例取 UUID。
2. **`doc_replace_text` 的语义是「同一个 text 替换多个 ranges」，不是每个 range 各自的文本**。要逐段写不同内容必须**降序单次调用**（本次 20 段用了 20 次调用）。返回 `{"message":"replace_text ok"}` 就是成功 —— 别用 `"ok": true` 判断（会全部误判为失败）。
3. **`edsdk.py schema doc_apply_named_style` 会崩**（`'list' object has no attribute 'get'`，服务端 schema 序列化 bug）。改段落样式改用 `doc_modify_paragraph` + `heading_lvl`（注意 heading_lvl/jc/spacing 可同包，但与 numbering/indent/block_quote 互斥）。
4. **模板 `.doc` 经 editor_sdk 保存后内容变成 OOXML（ZIP，魔数 `PK\x03\x04`）**，扩展名仍是 `.doc` 会让 Word 报"格式与扩展名不匹配"。**必须改扩展名为 `.docx`**。原始 `.doc` 是 OLE2（`D0 CF 11 E0`），112128 字节 → 保存后 37651 字节。
5. 追加多张同结构表的最稳流程：`doc_get_last_operable_pos` → `doc_insert_paragraph_with_text(idx=pos, level=N)` → 再取一次 `last_operable_pos` → `doc_insert_table(idx=pos2, rows, cols)` → `doc_set_table_cells`。
6. 表格 `table_id` **重新打开文档后会变**（模板里 687slacg → 副本里 c0ly1g3o），每次操作前重新 `doc_list_tables`/`doc_get_table_info` 取。
7. 迁移章节（如把 3.5 挪到文末）的安全顺序：先在末尾追加新内容 → 再 `doc_delete_paragraph` 删旧段落（此时旧末段已不是全文最后一段，才允许删除）→ 最后重建被迁移的章节。

**未提交任何 git commit/push**，HEAD 仍 `0c401fe`。

---

## 代码还原 + 完成《LLMGal_测试缺陷报告书.docx》

- 应要求把源码层全部还原：`backend/Voice.py`、`backend/Integration.py`、`backend/tests/test_service_and_state_cases.py` 全部 `git checkout` 回原状，脚本重跑回到基线 **32 passed / 2 xfailed**，`backend/` diff 归零。
- 交付物 `docs/module1/LLMGal_测试缺陷报告书.docx`（34390 字节，13 张表）：修订记录表 + 参考资料表（5 条）+ 001 模板原表 + 002–011 新建表，共 11 条缺陷。
- 工作区最终只剩 `M scripts/run_module1_tests.sh`（策略 A 的脚本加固，按用户要求保留待其审核）。

**这一轮踩到并解决的关键问题（接上编号）**

8. **新建表未做单元格合并 → 值落在非合并起始格，渲染时视觉丢失**。模板表靠 `gridSpan` 把 11 列合成 4/4/2/2/2/6/2/2/2/1/2/2/2/1/4/4/2/2 个渲染格；新插入的表是裸 11 列，写入列号与模板对不上。用 MCP 逐格 `doc_merge_table_cells` 修既慢又脆（10 表 × 十几处合并）。
9. **终选方案：深拷贝模板表 + 直接改 XML**。`zipfile` 读 `word/document.xml` → `ElementTree` 解析 → 拿 body 里 idx=2 的 `<w:tbl>`（模板原表，自带合并）`deepcopy` 出 10 份替换 idx=3..12 → 按「网格列起点」写文本（只写合并起始格）→ 序列化回写。合并、列宽、字体、行高全部与模板一致。
10. **序列化必须回填原始根标签**。`ET.tostring` 会重排甚至丢掉命名空间声明，而 `mc:Ignorable="w14 wp14"` 是按前缀字符串引用的。做法：正则从原文抠出 `<?xml ...?>` 与 `<w:document ...>` 开标签，`ET.register_namespace` 注册全部前缀，序列化后把根标签原样替换回去。校验：新旧根标签字符串相等 + 重新 `ET.fromstring` 通过 + 编辑器能打开。
11. **`doc_replace_text` 会破坏多行单元格**。001 表的「详细描述」被拆成 `详细描述 / 1. / 2.  / 预期结果： / 实际结果：` 碎片，中间正文丢失。**多行正文一律重写整格，不要增量替换**。
12. **目录（TOC）缓存条目不会自动跟正文**。目录是真正的域（` TOC \o "1-3" \h \z `；`body[18]` 是 `fldChar begin`，`body[34]` 是 `end`，中间每段一个条目），缓存里仍是模板示例（"3.4.1 管理员"、"表3.4.1 001-添加单个教师用户…"）。两个修正：① 改缓存标签（定位方式：找 `fldChar separate` 之后、含 `w:tab` 之前的 `<w:t>` 即条目可见文本）并补 10 条；② 在 `word/settings.xml` 的 `<w:footnotePr/>` 之前插入 `<w:updateFields w:val="true"/>`（CT_Settings 序列里的合法位置），Word/WPS 打开即自动刷新目录与页码。
13. **改磁盘前先 `close_file(force=true)`**，否则编辑器内存缓冲会在后续 `save_file` 时覆盖写入；改完再 `open_file` 复核（`open_file` 是流式异步的，紧接着查 `doc_list_tables` 会报 `document is not opened`，等几秒即可）。
14. 环境类：沙箱内置 Bash 的 PATH 缺 coreutils（`ls`/`head`/`tail`/`dirname`/`wc` 全不可用），一切 shell 操作走 `"/c/Program Files/Git/bin/bash.exe" -c '...'`；`--json-file` 传 MSYS 路径（`/c/...`）原生 Python 不认，必须传 `C:/...`；本机托管 Python 3.13 没有 `python-docx`/`lxml`，本次全程只用标准库。

**未提交任何 git commit/push**，HEAD 仍 `0c401fe`。

---

## 全项目前后端扫描：现存缺陷清单报告（2026-09-12 晚）

产出 `docs/module1/LLMGal_前后端现存缺陷清单报告.md`（285 行 / 32.5 KB，8 章）。
扫描范围：`backend/` 14 个 .py（约 1900 行）+ `frontend/src/` 21 个源码文件（约 4600 行）+ 构建与测试配置。**未改任何源码**。

**结论：65 条缺陷**（后端 32 / 前端 33；P0 6、P1 17、P2 24、P3 18）。

### 扫描出的几条高价值结论（下次别重复排查）

1. **前端 markdown 的公式规则会吃掉所有链接与图片**：`markdown.ts:70` 的 `/\[([^\]]+)\]/` 注册在 `before('escape')`，优先级高于 markdown-it 内置 `link`/`image`，`[文本](url)` 里的 `[文本]` 被抢先切成 KaTeX 公式。ChatInput 的图片附件与 `formatImageMessage` 全都渲染不出来。
2. **前端错误消息 100% 丢失的闭环**：后端 `send_error`（`Connect.py:293-298`）不带 `message_id` → 前端 `isServerMessage` 要求 `typeof message_id === "string"` → 判为非法 → `emit('invalid_message')`，而**该事件无人监听**。所以"点了发送没反应"。
3. **ACK 吞消息**：`Connect.py:269` 无条件 `await websocket.receive_json()`，用户连发两条时第二条被当 ACK 吃掉且不重放；`timeout=30` 还会卡住整个循环。
4. **静态模式清空图片 URL**：`Image.static_images` 资源齐全时返回 `""` → `updateLinks(role, "", idx)` 把 Records 的 `Recent_Url` 覆写为空 → 下次实时渲染必失败并回退付费文生图。TC-IMG-01 的断言恰好固化了这个行为。
5. **`Text.py:35` `full_prompt.append(prompt)`**：前端两条链路一个传字符串（`UseChatClient.ts:77`）一个传对象（`ChatView.vue:501`），传字符串必失败。另 `Text.py:54` `if delta.content != "":` 在推理模型首 chunk `content=None` 时 `+= None` 抛 TypeError。
6. **`useChatClient` / `LLMClient` 全项目零引用**（已 grep 确认）。该链路里的缺陷（`WebSocketManager.off` 失效、`imageMode_config` 命名、`cancelRequest` 语义、`handleError` 单参调用）**当前不会发作**，但会误导维护者。实际通信走 `ChatView.vue` 里直接 `new WebSocketManager(...)`。
7. **两组测试在做"错误的事"**：`xfail(strict=True)` 钉住 2 条（TC-TTS-04 非法 Base64、TC-EX-02 正则过严）；另有 8 条用例**通过的方式是在断言缺陷行为**（TC-CON-01 并发同槽、TC-EX-01 抛 IndexError、TC-RES-03 抛 FileNotFoundError、TC-LLM-01 用 assistant 装 system prompt、TC-IMG-01 返回空 URL、TC-IMG-02 缺 1 张重生 7 张、TC-TTS-02 失败仍 success、TC-WS-05/06 用 KeyError 而非显式校验）。**修 D10/D11/D12/D13 时必须同步改断言，否则"修好了测试反而变红"。**
8. **覆盖率盲区 = 缺陷高发区**：Voice.py 31%、Image.py 40%、Connect.py 73%、Integration.py 83%，TOTAL 56%。未覆盖行号与缺陷落点高度重合（如 Integration 缺的 `67-80`/`129-132`/`151-154` 正是 Records 写回与异常回滚）。
9. **凭据硬编码 6 处**：`Connect.py:42`、`Voice.py:17-18/116/213`（同一 token 重复 3 次）、`Text.py:38`（第三方中转站 o3.fan）、`Image.py:9-10`、`image_emotions.py:4-5`。密钥已进 git 历史，仅改源码不够，须轮换。
10. **13 处相对路径** `../frontend/src/assets/...`（Integration 3、Text 1、Voice 2、Image 5、…），依赖启动 CWD；测试靠 `monkeypatch.chdir()` 绕开，掩盖了问题。

### 报告里刻意剔除的误报（下次别再报）

- `ChatView.vue:504/506` 的 `modelText.value` / `role.value` **不是** ref 误用——`modelText` 是 `ModelOption` 对象，`.value` 正是其模型标识字段，结果正确。
- `ChatMessage.vue:188` 的 `<Loading />` 未 import，但 `main.ts:18-20` 全局注册了所有 Element Plus 图标，能正常解析。
- `Image.static_images` 内部命名自洽（它给 `emotional_bro` 传的第 4 参是情绪名），"反复重生"只在情绪名文件被删后发生，不能算"每次烧钱"。

### 本次未做的事

- 未改任何源码；**未提交任何 git commit/push**，HEAD 仍 `0c401fe`。
- 工作区改动仍只有 `scripts/run_module1_tests.sh` 一项（策略 A 的加固，待审核）。

---

## 21:00 一阶段 mock 测试修复（用户指令：先不碰 AI 接入）

**目标**：让模块一隔离测试全绿。范围严格限定为"两条 xfail 的根因"，不扩散到 AI / 网络接入层。

**改动 3 处（后端 2 + 测试 1）**
1. `backend/Voice.py:191` `save_audio_from_base64`：先 `"".join(audio.split())` 剥离合法折行空白，再 `b64decode(..., validate=True)`；解出空字节直接 `return ""`。解码与校验提到 `makedirs` **之前**，非法输入不再产生任何文件副作用。
2. `backend/Integration.py:119` Records 解析正则：`index:(\d+)` → `index:([^\n]*)`，让 `index:abc` 能被取出，在 `int(index)` 处以 ValueError 失败（异常类型/位置符合用例预期），而不是提前失配抛 IndexError。
3. `backend/tests/test_service_and_state_cases.py`：删掉 TC-TTS-04、TC-EX-02 两行 `@pytest.mark.xfail(strict=True)`。

**为什么第 3 步是强制的**：strict xfail 下用例一旦通过 = XPASS = 判定 FAILED。修了源码却不删标记，脚本反而更红。

**实测（`./scripts/run_module1_tests.sh`）**
- 修复前：`32 passed, 2 xfailed`（556 stmts / 243 miss / 56%）
- 修复后：**`34 passed`**（560 stmts / 242 miss / **57%**；Voice 160/111/31% → 164/110/33%）
- 前端 `npm test -- --run`：1 passed（vitest 3.2.4）
- 其余 32 条无回归（进度点 20 + 14 全绿，退出码 0）

**同步更新**：`README.md` 中"当前原始源码的基线结果为 32 passed, 2 xfailed"改为 34 passed 并说明标记已移除——原表述在修复后已失真。

**刻意未动**：`Text.py`、`Image.py`、`Connect.py`、前端全部源码原样。AI 接入与 WebSocket 契约层按用户要求留待后续指令。

**未提交任何 git commit/push**，HEAD 仍 `0c401fe`。工作区：`M backend/Integration.py`、`M backend/Voice.py`、`M backend/tests/test_service_and_state_cases.py`、`M scripts/run_module1_tests.sh`、`M README.md`。

**已有报告仍准确**：`docs/module1/LLMGal_backend_bug修复对比报告.md` 描述的就是这三处，实测数据与本次一致，无需重写。
